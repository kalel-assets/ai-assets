# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build output for branch-based Pages (npm run build:docs). This repo deploys via
# Actions, so it stays untracked here. An internal mirror that cannot run Actions
# must DELETE this line — there, committing docs/ is the whole deployment.
docs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cygwin/MSYS crash dumps
*.stackdump
